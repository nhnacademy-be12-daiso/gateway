spring:
  application:
    name: team3-gateway

  data:
    redis:
      # docker-compose.yml의 SPRING_DATA_REDIS_HOST 값
      host: s4.java21.net
      # docker-compose.yml의 SPRING_DATA_REDIS_PORT 값
      port: 6379
      # docker-compose.yml의 SPRING_DATA_REDIS_PASSWORD 값
      password: '*N2vya7H@muDTwdNMR!'
      # docker-compose.yml의 SPRING_DATA_REDIS_DATABASE 값
      database: 370

  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
#      defaultZone: http://team3-eureka:8761/eureka/
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: c3ByaW5nLWJvb3Qtc2VjdXJpdHktand0LXR1dG9yaWFsLXNlY3JldC1rZXktZ2VuZXJhdGVkLWJ5LWJhc2U2NC1lbmNvZGVyLXRlc3QtMTIzNDU2Nzg5MA==
  expiration-time: 3600000
  token-prefix: Bearer
  header: Authorization

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
